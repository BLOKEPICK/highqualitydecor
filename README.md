# High Quality Decor (Next.js 14 — App Router)

Bootstrapped from a component-only ZIP.

## Run locally
```bash
npm install
npm run dev
# open http://localhost:3000
```

## Scripts
- `dev`: Run development server
- `build`: Production build
- `start`: Start server after build
- `lint`: Lint

## Notes
- Uses `app/` directory (App Router).
- Place images in `public/` and reference as `/image.png`.
- If you deploy on Vercel, no extra config is required.
