/** app/page.js (PATCH) — Use UA-based server wrapper; avoid client toggles */
import HeroUAWrapper from "../components/HeroUAWrapper";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function Home() {
  return (
    <main>
      <HeroUAWrapper />
      {/* ...rest of your sections... */}
    </main>
  );
}
