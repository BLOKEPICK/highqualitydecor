// app/layout.js (PATCH) — Inject critical CSS for mobile/desktop (pre-paint)
// Paste this <style> inside your Layout component's return, near <body> or before {children}.
{/* BEGIN: critical CSS for hero toggles */}
<style
  // Plain <style> (not styled-jsx); valid in Server Components
  dangerouslySetInnerHTML={{
    __html: \`
.mobile { display: none; }
.desktop { display: block; }
@media (max-width: 1023px) {
  .mobile { display: block; }
  .desktop { display: none; }
}
\`
  }}
/>
{/* END: critical CSS for hero toggles */}
