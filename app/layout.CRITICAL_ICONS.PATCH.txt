/* app/layout.js (PATCH) — CSS crítico para evitar flashes */
<style
  dangerouslySetInnerHTML={{
    __html: `
/* Asegura tamaños de íconos antes de hidratar */
.socials svg, .social-inline svg { width:22px; height:22px; display:block; }
.chip-ico { width:16px; height:16px; display:block; }
`
  }}
/>
